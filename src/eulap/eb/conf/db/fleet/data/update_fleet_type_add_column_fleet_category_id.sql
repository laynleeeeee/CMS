
-- Description : Script to add FLEET_CATEGORY_ID column on FLEET_TYPE table.

ALTER TABLE FLEET_TYPE ADD COLUMN FLEET_CATEGORY_ID int(10) unsigned DEFAULT NULL AFTER FLEET_TYPE_ID, 
ADD CONSTRAINT FK_FLEET_TYPE_CATEGORY_ID FOREIGN KEY (FLEET_CATEGORY_ID) REFERENCES FLEET_CATEGORY (FLEET_CATEGORY_ID);