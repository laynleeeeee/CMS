
#-- Description: ArMiscellaneous - NSB 5 configuration.

ArMiscellaneous4.typeId=1
ArMiscellaneous4.form=arMiscellaneous/form/viewForm
ArMiscellaneous4.edit=ar_miscellaneous_nsb5_edit
ArMiscellaneous4.print=arMiscellaneous/pdf
ArMiscellaneous4.service=eulap.eb.service.ArMiscellaneousService
ArMiscellaneous4.acct_transaction_types=1
ArMiscellaneous4.oo.parent_to_child=1
ArMiscellaneous4.flow=1;2;3;
ArMiscellaneous4.1.statusId=1
ArMiscellaneous4.1.required=
ArMiscellaneous4.1.next=2
ArMiscellaneous4.1.product_code=24081
ArMiscellaneous4.1.module_code=8
ArMiscellaneous4.2.statusId=30
ArMiscellaneous4.2.required=1
ArMiscellaneous4.2.next=3
ArMiscellaneous4.2.product_code=24081
ArMiscellaneous4.2.module_code=16
ArMiscellaneous4.3.statusId=16
ArMiscellaneous4.3.required=2
ArMiscellaneous4.3.next=0
ArMiscellaneous4.3.product_code=24081
ArMiscellaneous4.3.module_code=32
ArMiscellaneous4.sql.entries=1;2;3;4;5;6;7
#-- Debit debit account combination
ArMiscellaneous4.sql.1.je=SELECT 'OTHER RECEIPT - NSB 5' as SOURCE, AM.AR_MISCELLANEOUS_ID as ID, AC.COMPANY_ID, AC.DIVISION_ID, AC.ACCOUNT_ID, D.NUMBER AS DIVISION_NO, D.NAME AS DIVISION_NAME, \
	A.NUMBER as ACCT_NO, A.ACCOUNT_NAME, AM.MATURITY_DATE as GL_DATE, AM.RECEIPT_NUMBER AS REFERENCE_NUMBER, AM.DESCRIPTION, AM.AMOUNT AS DEBIT, 0 as CREDIT, \
	FW.FORM_WORKFLOW_ID, FW.CURRENT_STATUS_ID, FW.IS_COMPLETE, FW.IS_COMPLETE as IS_POSTED, '' AS CUSTOMER_PO_NO, A.ACCOUNT_TYPE_ID \
	FROM AR_MISCELLANEOUS AM \
	INNER JOIN RECEIPT_METHOD RM ON RM.RECEIPT_METHOD_ID = AM.RECEIPT_METHOD_ID \
	INNER JOIN ACCOUNT_COMBINATION AC ON AC.ACCOUNT_COMBINATION_ID = RM.DEBIT_ACCOUNT_COMBINATION_ID \
	INNER JOIN DIVISION D ON D.DIVISION_ID = AC.DIVISION_ID \
	INNER JOIN ACCOUNT A ON A.ACCOUNT_ID = AC.ACCOUNT_ID \
	INNER JOIN FORM_WORKFLOW FW ON AM.FORM_WORKFLOW_ID = FW.FORM_WORKFLOW_ID \
	WHERE FW.CURRENT_STATUS_ID != 4 \
	AND RM.DEBIT_ACCOUNT_COMBINATION_ID IS NOT NULL \
	AND AM.DIVISION_ID = 4 \
	AND AC.COMPANY_ID = IN_COMPANY_ID
#-- Debit, bank account
ArMiscellaneous4.sql.2.je=SELECT 'OTHER RECEIPT - NSB 5' as SOURCE, AM.AR_MISCELLANEOUS_ID as ID, AC.COMPANY_ID, AC.DIVISION_ID, AC.ACCOUNT_ID, D.NUMBER AS DIVISION_NO, D.NAME AS DIVISION_NAME, \
	A.NUMBER as ACCT_NO, A.ACCOUNT_NAME, AM.MATURITY_DATE as GL_DATE, AM.RECEIPT_NUMBER AS REFERENCE_NUMBER, AM.DESCRIPTION, AM.AMOUNT AS DEBIT, 0 as CREDIT, \
	FW.FORM_WORKFLOW_ID, FW.CURRENT_STATUS_ID, FW.IS_COMPLETE, FW.IS_COMPLETE as IS_POSTED, '' AS CUSTOMER_PO_NO, A.ACCOUNT_TYPE_ID \
	FROM AR_MISCELLANEOUS AM \
	INNER JOIN RECEIPT_METHOD RM ON RM.RECEIPT_METHOD_ID = AM.RECEIPT_METHOD_ID \
	INNER JOIN BANK_ACCOUNT BA ON BA.BANK_ACCOUNT_ID = RM.BANK_ACCOUNT_ID \
	INNER JOIN ACCOUNT_COMBINATION AC ON AC.ACCOUNT_COMBINATION_ID = BA.CASH_IN_BANK_ACCT_ID \
	INNER JOIN DIVISION D ON D.DIVISION_ID = AC.DIVISION_ID \
	INNER JOIN ACCOUNT A ON A.ACCOUNT_ID = AC.ACCOUNT_ID \
	INNER JOIN FORM_WORKFLOW FW ON AM.FORM_WORKFLOW_ID = FW.FORM_WORKFLOW_ID \
	WHERE FW.CURRENT_STATUS_ID != 4 \
	AND RM.BANK_ACCOUNT_ID IS NOT NULL \
	AND AM.DIVISION_ID = 4 \
	AND AC.COMPANY_ID = IN_COMPANY_ID
#-- Debit
ArMiscellaneous4.sql.3.je=SELECT 'OTHER RECEIPT - NSB 5' as SOURCE, AM.AR_MISCELLANEOUS_ID as ID, AC.COMPANY_ID, AC.DIVISION_ID, AC.ACCOUNT_ID, D.NUMBER AS DIVISION_NO, D.NAME AS DIVISION_NAME, \
	A.NUMBER as ACCT_NO, A.ACCOUNT_NAME, AM.MATURITY_DATE as GL_DATE, AM.RECEIPT_NUMBER AS REFERENCE_NUMBER, AM.DESCRIPTION, -AML.AMOUNT AS DEBIT, 0 as CREDIT, \
	FW.FORM_WORKFLOW_ID, FW.CURRENT_STATUS_ID, FW.IS_COMPLETE, FW.IS_COMPLETE as IS_POSTED, '' AS CUSTOMER_PO_NO, A.ACCOUNT_TYPE_ID \
	FROM AR_MISCELLANEOUS_LINE AML \
	INNER JOIN AR_MISCELLANEOUS AM ON AM.AR_MISCELLANEOUS_ID = AML.AR_MISCELLANEOUS_ID \
	INNER JOIN SERVICE_SETTING ALS ON ALS.SERVICE_SETTING_ID = AML.SERVICE_SETTING_ID \
	INNER JOIN ACCOUNT_COMBINATION AC ON AC.ACCOUNT_COMBINATION_ID = ALS.ACCOUNT_COMBINATION_ID \
	INNER JOIN DIVISION D ON D.DIVISION_ID = AC.DIVISION_ID \
	INNER JOIN ACCOUNT A ON A.ACCOUNT_ID = AC.ACCOUNT_ID \
	INNER JOIN FORM_WORKFLOW FW ON AM.FORM_WORKFLOW_ID = FW.FORM_WORKFLOW_ID \
	WHERE FW.CURRENT_STATUS_ID != 4 \
	AND AM.DIVISION_ID = 4 \
	AND AML.AMOUNT < 0 \
	AND AC.COMPANY_ID = IN_COMPANY_ID
#-- Credit
ArMiscellaneous4.sql.4.je=SELECT 'OTHER RECEIPT - NSB 5' as SOURCE, AM.AR_MISCELLANEOUS_ID as ID, AC.COMPANY_ID, AC.DIVISION_ID, AC.ACCOUNT_ID, D.NUMBER AS DIVISION_NO, D.NAME AS DIVISION_NAME, \
	A.NUMBER as ACCT_NO, A.ACCOUNT_NAME, AM.MATURITY_DATE as GL_DATE, AM.RECEIPT_NUMBER AS REFERENCE_NUMBER, AM.DESCRIPTION, 0 AS DEBIT, AML.AMOUNT as CREDIT, \
	FW.FORM_WORKFLOW_ID, FW.CURRENT_STATUS_ID, FW.IS_COMPLETE, FW.IS_COMPLETE as IS_POSTED, '' AS CUSTOMER_PO_NO, A.ACCOUNT_TYPE_ID \
	FROM AR_MISCELLANEOUS_LINE AML \
	INNER JOIN AR_MISCELLANEOUS AM ON AM.AR_MISCELLANEOUS_ID = AML.AR_MISCELLANEOUS_ID \
	INNER JOIN SERVICE_SETTING ALS ON ALS.SERVICE_SETTING_ID = AML.SERVICE_SETTING_ID \
	INNER JOIN ACCOUNT_COMBINATION AC ON AC.ACCOUNT_COMBINATION_ID = ALS.ACCOUNT_COMBINATION_ID \
	INNER JOIN DIVISION D ON D.DIVISION_ID = AC.DIVISION_ID \
	INNER JOIN ACCOUNT A ON A.ACCOUNT_ID = AC.ACCOUNT_ID \
	INNER JOIN FORM_WORKFLOW FW ON AM.FORM_WORKFLOW_ID = FW.FORM_WORKFLOW_ID \
	WHERE FW.CURRENT_STATUS_ID != 4 \
	AND AM.DIVISION_ID = 4 \
	AND AML.AMOUNT >= 0 \
	AND AC.COMPANY_ID = IN_COMPANY_ID
# VAT
ArMiscellaneous4.sql.5.je=SELECT 'OTHER RECEIPT - NSB 5' as SOURCE, AM.AR_MISCELLANEOUS_ID as ID, AC.COMPANY_ID, AC.DIVISION_ID, AC.ACCOUNT_ID, D.NUMBER AS DIVISION_NO, D.NAME AS DIVISION_NAME, \
	A.NUMBER as ACCT_NO, A.ACCOUNT_NAME, AM.MATURITY_DATE as GL_DATE, AM.RECEIPT_NUMBER AS REFERENCE_NUMBER, AM.DESCRIPTION, 0 AS DEBIT, COALESCE(AML.VAT_AMOUNT, 0) as CREDIT, \
	FW.FORM_WORKFLOW_ID, FW.CURRENT_STATUS_ID, FW.IS_COMPLETE, FW.IS_COMPLETE as IS_POSTED, '' AS CUSTOMER_PO_NO, A.ACCOUNT_TYPE_ID \
	FROM AR_MISCELLANEOUS_LINE AML \
	INNER JOIN AR_MISCELLANEOUS AM ON AM.AR_MISCELLANEOUS_ID = AML.AR_MISCELLANEOUS_ID \
	INNER JOIN AR_CUSTOMER_ACCOUNT ACA ON ACA.AR_CUSTOMER_ACCOUNT_ID = AM.AR_CUSTOMER_ACCOUNT_ID \
	INNER JOIN VAT_ACCOUNT_SETUP VAS ON IF(AM.DIVISION_ID IS NOT NULL, \
	(VAS.COMPANY_ID = AM.COMPANY_ID AND VAS.DIVISION_ID = AM.DIVISION_ID), VAS.COMPANY_ID = AM.COMPANY_ID) \
	INNER JOIN ACCOUNT_COMBINATION AC ON AC.ACCOUNT_COMBINATION_ID = VAS.OUTPUT_VAT_AC_ID \
	INNER JOIN DIVISION D ON D.DIVISION_ID = AC.DIVISION_ID \
	INNER JOIN ACCOUNT A ON A.ACCOUNT_ID = AC.ACCOUNT_ID \
	INNER JOIN FORM_WORKFLOW FW ON AM.FORM_WORKFLOW_ID = FW.FORM_WORKFLOW_ID \
	WHERE FW.CURRENT_STATUS_ID != 4 \
	AND AM.DIVISION_ID = 4 \
	AND AML.VAT_AMOUNT > 0 \
	AND VAS.TAX_TYPE_ID = AML.TAX_TYPE_ID \
	AND AC.COMPANY_ID = IN_COMPANY_ID
# Withholding Tax
ArMiscellaneous4.sql.6.je=SELECT 'OTHER RECEIPT - NSB 5' as SOURCE, AM.AR_MISCELLANEOUS_ID as ID, AC.COMPANY_ID, AC.DIVISION_ID, AC.ACCOUNT_ID, D.NUMBER AS DIVISION_NO, D.NAME AS DIVISION_NAME, \
	A.NUMBER as ACCT_NO, A.ACCOUNT_NAME, AM.MATURITY_DATE as GL_DATE, AM.RECEIPT_NUMBER AS REFERENCE_NUMBER, AM.DESCRIPTION, COALESCE(AM.WT_AMOUNT, 0) AS DEBIT, 0 as CREDIT, \
	FW.FORM_WORKFLOW_ID, FW.CURRENT_STATUS_ID, FW.IS_COMPLETE, FW.IS_COMPLETE as IS_POSTED, '' AS CUSTOMER_PO_NO, A.ACCOUNT_TYPE_ID \
	FROM AR_MISCELLANEOUS AM \
	INNER JOIN WT_ACCOUNT_SETTING WTAS ON WTAS.WT_ACCOUNT_SETTING_ID = AM.WT_ACCOUNT_SETTING_ID \
	INNER JOIN ACCOUNT_COMBINATION AC ON AC.ACCOUNT_COMBINATION_ID = WTAS.ACCOUNT_COMBINATION_ID \
	INNER JOIN DIVISION D ON D.DIVISION_ID = AC.DIVISION_ID \
	INNER JOIN ACCOUNT A ON A.ACCOUNT_ID = AC.ACCOUNT_ID \
	INNER JOIN FORM_WORKFLOW FW ON AM.FORM_WORKFLOW_ID = FW.FORM_WORKFLOW_ID \
	WHERE FW.CURRENT_STATUS_ID != 4 \
	AND AM.DIVISION_ID = 4 \
	AND AM.WT_ACCOUNT_SETTING_ID IS NOT NULL \
	AND AC.COMPANY_ID = IN_COMPANY_ID
# Withholding VAT
ArMiscellaneous4.sql.7.je=SELECT 'OTHER RECEIPT - NSB 5' as SOURCE, AM.AR_MISCELLANEOUS_ID as ID, AC.COMPANY_ID, AC.DIVISION_ID, AC.ACCOUNT_ID, D.NUMBER AS DIVISION_NO, D.NAME AS DIVISION_NAME, \
	A.NUMBER as ACCT_NO, A.ACCOUNT_NAME, AM.MATURITY_DATE as GL_DATE, AM.RECEIPT_NUMBER AS REFERENCE_NUMBER, AM.DESCRIPTION, COALESCE(AM.WT_VAT_AMOUNT, 0) AS DEBIT, 0 as CREDIT, \
	FW.FORM_WORKFLOW_ID, FW.CURRENT_STATUS_ID, FW.IS_COMPLETE, FW.IS_COMPLETE as IS_POSTED, '' AS CUSTOMER_PO_NO, A.ACCOUNT_TYPE_ID \
	FROM AR_MISCELLANEOUS AM \
	INNER JOIN AR_MISCELLANEOUS_ACCOUNT ARMA ON (ARMA.COMPANY_ID = AM.COMPANY_ID AND ARMA.DIVISION_ID = AM.DIVISION_ID) \
	INNER JOIN ACCOUNT_COMBINATION AC ON AC.ACCOUNT_COMBINATION_ID = ARMA.WT_VAT_AC_ID \
	INNER JOIN DIVISION D ON D.DIVISION_ID = AC.DIVISION_ID \
	INNER JOIN ACCOUNT A ON A.ACCOUNT_ID = AC.ACCOUNT_ID \
	INNER JOIN FORM_WORKFLOW FW ON AM.FORM_WORKFLOW_ID = FW.FORM_WORKFLOW_ID \
	WHERE FW.CURRENT_STATUS_ID != 4 \
	AND AM.DIVISION_ID = 4 \
	AND AM.WT_VAT_AMOUNT > 0 \
	AND AC.COMPANY_ID = IN_COMPANY_ID
ArMiscellaneous4.sql.je.jerr=AND AM.MATURITY_DATE BETWEEN DATE_FROM and DATE_TO \
	AND (CASE WHEN IN_REFERENCE_NUMBER NOT LIKE '-1' THEN AM.RECEIPT_NUMBER like IN_REFERENCE_NUMBER \
		ELSE IN_REFERENCE_NUMBER like IN_REFERENCE_NUMBER END) \
	AND (CASE IN_SOURCE WHEN 'OTHER RECEIPT - NSB 5' THEN AM.AR_MISCELLANEOUS_ID != 0 \
		WHEN 'ALL' THEN AM.AR_MISCELLANEOUS_ID != 0 \
		ELSE AM.AR_MISCELLANEOUS_ID = 0 END) \
	AND (CASE WHEN IN_IS_POSTED != -1 THEN FW.IS_COMPLETE = IN_IS_POSTED ELSE FW.IS_COMPLETE != IN_IS_POSTED END)
ArMiscellaneous4.sql.je.ab=AND IF(DATE_FROM IS NOT NULL, AM.MATURITY_DATE BETWEEN DATE_FROM AND DATE_TO, AM.MATURITY_DATE <= DATE_TO) \
	AND IF(IN_DIVISION_ID != -1, AM.DIVISION_ID = IN_DIVISION_ID, AM.DIVISION_ID != IN_DIVISION_ID) \
	AND FW.IS_COMPLETE = 1
ArMiscellaneous4.sql.je.aa=AND D.NUMBER BETWEEN IN_FROM_DIVISION AND IN_TO_DIVISION \
	AND AC.ACCOUNT_ID = IN_ACCOUNT_ID \
	AND AM.MATURITY_DATE BETWEEN DATE_FROM AND DATE_TO \
	AND AM.DESCRIPTION LIKE IN_DESCRIPTION \
	AND FW.IS_COMPLETE = 1
ArMiscellaneous4.sql.je.gll=AND AC.ACCOUNT_ID = IN_ACCOUNT_ID \
	AND AM.MATURITY_DATE BETWEEN DATE_FROM AND DATE_TO \
	AND FW.IS_COMPLETE = 1
# Configuration for IS and BS by division
#-- Debit debit account combination
ArMiscellaneous4.sql.1.isbs=SELECT AC.COMPANY_ID, D.NAME AS DIVISION_NAME, D.DIVISION_ID, A.ACCOUNT_NAME, A.ACCOUNT_ID, A.PARENT_ACCOUNT_ID, \
	AM.AMOUNT AS DEBIT, 0 as CREDIT, A.ACCOUNT_TYPE_ID, A.NUMBER as ACCT_NO, AM.MATURITY_DATE AS DATE \
	FROM AR_MISCELLANEOUS AM \
	INNER JOIN RECEIPT_METHOD RM ON RM.RECEIPT_METHOD_ID = AM.RECEIPT_METHOD_ID \
	INNER JOIN ACCOUNT_COMBINATION AC ON AC.ACCOUNT_COMBINATION_ID = RM.DEBIT_ACCOUNT_COMBINATION_ID \
	INNER JOIN DIVISION D ON D.DIVISION_ID = AC.DIVISION_ID \
	INNER JOIN ACCOUNT A ON A.ACCOUNT_ID = AC.ACCOUNT_ID \
	INNER JOIN FORM_WORKFLOW FW ON AM.FORM_WORKFLOW_ID = FW.FORM_WORKFLOW_ID \
	WHERE FW.CURRENT_STATUS_ID != 4 \
	AND RM.DEBIT_ACCOUNT_COMBINATION_ID IS NOT NULL \
	AND AM.DIVISION_ID = 4 \
	AND AC.COMPANY_ID = IN_COMPANY_ID
#-- Debit, bank account
ArMiscellaneous4.sql.2.isbs=SELECT AC.COMPANY_ID, D.NAME AS DIVISION_NAME, D.DIVISION_ID, A.ACCOUNT_NAME, A.ACCOUNT_ID, A.PARENT_ACCOUNT_ID, \
	AM.AMOUNT AS DEBIT, 0 as CREDIT, A.ACCOUNT_TYPE_ID, A.NUMBER as ACCT_NO, AM.MATURITY_DATE AS DATE \
	FROM AR_MISCELLANEOUS AM \
	INNER JOIN RECEIPT_METHOD RM ON RM.RECEIPT_METHOD_ID = AM.RECEIPT_METHOD_ID \
	INNER JOIN BANK_ACCOUNT BA ON BA.BANK_ACCOUNT_ID = RM.BANK_ACCOUNT_ID \
	INNER JOIN ACCOUNT_COMBINATION AC ON AC.ACCOUNT_COMBINATION_ID = BA.CASH_IN_BANK_ACCT_ID \
	INNER JOIN DIVISION D ON D.DIVISION_ID = AC.DIVISION_ID \
	INNER JOIN ACCOUNT A ON A.ACCOUNT_ID = AC.ACCOUNT_ID \
	INNER JOIN FORM_WORKFLOW FW ON AM.FORM_WORKFLOW_ID = FW.FORM_WORKFLOW_ID \
	WHERE FW.CURRENT_STATUS_ID != 4 \
	AND RM.BANK_ACCOUNT_ID IS NOT NULL \
	AND AM.DIVISION_ID = 4 \
	AND AC.COMPANY_ID = IN_COMPANY_ID
#-- Debit
ArMiscellaneous4.sql.3.isbs=SELECT AC.COMPANY_ID, D.NAME AS DIVISION_NAME, D.DIVISION_ID, A.ACCOUNT_NAME, A.ACCOUNT_ID, A.PARENT_ACCOUNT_ID, \
	-AML.AMOUNT AS DEBIT, 0 as CREDIT, A.ACCOUNT_TYPE_ID, A.NUMBER as ACCT_NO, AM.MATURITY_DATE AS DATE \
	FROM AR_MISCELLANEOUS_LINE AML \
	INNER JOIN AR_MISCELLANEOUS AM ON AM.AR_MISCELLANEOUS_ID = AML.AR_MISCELLANEOUS_ID \
	INNER JOIN SERVICE_SETTING ALS ON ALS.SERVICE_SETTING_ID = AML.SERVICE_SETTING_ID \
	INNER JOIN ACCOUNT_COMBINATION AC ON AC.ACCOUNT_COMBINATION_ID = ALS.ACCOUNT_COMBINATION_ID \
	INNER JOIN DIVISION D ON D.DIVISION_ID = AC.DIVISION_ID \
	INNER JOIN ACCOUNT A ON A.ACCOUNT_ID = AC.ACCOUNT_ID \
	INNER JOIN FORM_WORKFLOW FW ON AM.FORM_WORKFLOW_ID = FW.FORM_WORKFLOW_ID \
	WHERE FW.CURRENT_STATUS_ID != 4 \
	AND AM.DIVISION_ID = 4 \
	AND AML.AMOUNT < 0 \
	AND AC.COMPANY_ID = IN_COMPANY_ID
#-- Credit
ArMiscellaneous4.sql.4.isbs=SELECT AC.COMPANY_ID, D.NAME AS DIVISION_NAME, D.DIVISION_ID, A.ACCOUNT_NAME, A.ACCOUNT_ID, A.PARENT_ACCOUNT_ID, \
	0 AS DEBIT, AML.AMOUNT as CREDIT, A.ACCOUNT_TYPE_ID, A.NUMBER as ACCT_NO, AM.MATURITY_DATE AS DATE \
	FROM AR_MISCELLANEOUS_LINE AML \
	INNER JOIN AR_MISCELLANEOUS AM ON AM.AR_MISCELLANEOUS_ID = AML.AR_MISCELLANEOUS_ID \
	INNER JOIN SERVICE_SETTING ALS ON ALS.SERVICE_SETTING_ID = AML.SERVICE_SETTING_ID \
	INNER JOIN ACCOUNT_COMBINATION AC ON AC.ACCOUNT_COMBINATION_ID = ALS.ACCOUNT_COMBINATION_ID \
	INNER JOIN DIVISION D ON D.DIVISION_ID = AC.DIVISION_ID \
	INNER JOIN ACCOUNT A ON A.ACCOUNT_ID = AC.ACCOUNT_ID \
	INNER JOIN FORM_WORKFLOW FW ON AM.FORM_WORKFLOW_ID = FW.FORM_WORKFLOW_ID \
	WHERE FW.CURRENT_STATUS_ID != 4 \
	AND AM.DIVISION_ID = 4 \
	AND AML.AMOUNT >= 0 \
	AND AC.COMPANY_ID = IN_COMPANY_ID
# VAT
ArMiscellaneous4.sql.5.isbs=SELECT AC.COMPANY_ID, D.NAME AS DIVISION_NAME, D.DIVISION_ID, A.ACCOUNT_NAME, A.ACCOUNT_ID, A.PARENT_ACCOUNT_ID, \
	0 AS DEBIT, COALESCE(AML.VAT_AMOUNT, 0) as CREDIT, A.ACCOUNT_TYPE_ID, A.NUMBER as ACCT_NO, AM.MATURITY_DATE AS DATE \
	FROM AR_MISCELLANEOUS_LINE AML \
	INNER JOIN AR_MISCELLANEOUS AM ON AM.AR_MISCELLANEOUS_ID = AML.AR_MISCELLANEOUS_ID \
	INNER JOIN AR_CUSTOMER_ACCOUNT ACA ON ACA.AR_CUSTOMER_ACCOUNT_ID = AM.AR_CUSTOMER_ACCOUNT_ID \
	INNER JOIN VAT_ACCOUNT_SETUP VAS ON IF(AM.DIVISION_ID IS NOT NULL, \
	(VAS.COMPANY_ID = AM.COMPANY_ID AND VAS.DIVISION_ID = AM.DIVISION_ID), VAS.COMPANY_ID = AM.COMPANY_ID) \
	INNER JOIN ACCOUNT_COMBINATION AC ON AC.ACCOUNT_COMBINATION_ID = VAS.OUTPUT_VAT_AC_ID \
	INNER JOIN DIVISION D ON D.DIVISION_ID = AC.DIVISION_ID \
	INNER JOIN ACCOUNT A ON A.ACCOUNT_ID = AC.ACCOUNT_ID \
	INNER JOIN FORM_WORKFLOW FW ON AM.FORM_WORKFLOW_ID = FW.FORM_WORKFLOW_ID \
	WHERE FW.CURRENT_STATUS_ID != 4 \
	AND AM.DIVISION_ID = 4 \
	AND AML.VAT_AMOUNT > 0 \
	AND VAS.TAX_TYPE_ID = AML.TAX_TYPE_ID \
	AND AC.COMPANY_ID = IN_COMPANY_ID
# Withholding Tax
ArMiscellaneous4.sql.6.isbs=SELECT AC.COMPANY_ID, D.NAME AS DIVISION_NAME, D.DIVISION_ID, A.ACCOUNT_NAME, A.ACCOUNT_ID, A.PARENT_ACCOUNT_ID, \
	COALESCE(AM.WT_AMOUNT, 0) AS DEBIT, 0 as CREDIT, A.ACCOUNT_TYPE_ID, A.NUMBER as ACCT_NO, AM.MATURITY_DATE AS DATE \
	FROM AR_MISCELLANEOUS AM \
	INNER JOIN WT_ACCOUNT_SETTING WTAS ON WTAS.WT_ACCOUNT_SETTING_ID = AM.WT_ACCOUNT_SETTING_ID \
	INNER JOIN ACCOUNT_COMBINATION AC ON AC.ACCOUNT_COMBINATION_ID = WTAS.ACCOUNT_COMBINATION_ID \
	INNER JOIN DIVISION D ON D.DIVISION_ID = AC.DIVISION_ID \
	INNER JOIN ACCOUNT A ON A.ACCOUNT_ID = AC.ACCOUNT_ID \
	INNER JOIN FORM_WORKFLOW FW ON AM.FORM_WORKFLOW_ID = FW.FORM_WORKFLOW_ID \
	WHERE FW.CURRENT_STATUS_ID != 4 \
	AND AM.DIVISION_ID = 4 \
	AND AM.WT_ACCOUNT_SETTING_ID IS NOT NULL \
	AND AC.COMPANY_ID = IN_COMPANY_ID
# Withholding VAT
ArMiscellaneous4.sql.7.isbs=SELECT AC.COMPANY_ID, D.NAME AS DIVISION_NAME, D.DIVISION_ID, A.ACCOUNT_NAME, A.ACCOUNT_ID, A.PARENT_ACCOUNT_ID, \
	COALESCE(AM.WT_VAT_AMOUNT, 0) AS DEBIT, 0 as CREDIT, A.ACCOUNT_TYPE_ID, A.NUMBER as ACCT_NO, AM.MATURITY_DATE AS DATE \
	FROM AR_MISCELLANEOUS AM \
	INNER JOIN AR_MISCELLANEOUS_ACCOUNT ARMA ON (ARMA.COMPANY_ID = AM.COMPANY_ID AND ARMA.DIVISION_ID = AM.DIVISION_ID) \
	INNER JOIN ACCOUNT_COMBINATION AC ON AC.ACCOUNT_COMBINATION_ID = ARMA.WT_VAT_AC_ID \
	INNER JOIN DIVISION D ON D.DIVISION_ID = AC.DIVISION_ID \
	INNER JOIN ACCOUNT A ON A.ACCOUNT_ID = AC.ACCOUNT_ID \
	INNER JOIN FORM_WORKFLOW FW ON AM.FORM_WORKFLOW_ID = FW.FORM_WORKFLOW_ID \
	WHERE FW.CURRENT_STATUS_ID != 4 \
	AND AM.DIVISION_ID = 4 \
	AND AM.WT_VAT_AMOUNT > 0 \
	AND AC.COMPANY_ID = IN_COMPANY_ID
ArMiscellaneous4.sql.bs=AND AM.MATURITY_DATE <= IN_AS_OF_DATE \
	AND FW.IS_COMPLETE = 1
ArMiscellaneous4.sql.is=AND AM.MATURITY_DATE BETWEEN IN_DATE_FROM AND IN_DATE_TO \
	AND FW.IS_COMPLETE = 1